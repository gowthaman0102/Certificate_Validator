/**
 * ShareModal.jsx — STUB (no longer used)
 * The share section is now rendered inline inside WalletCertCard,
 * matching the existing app's zero-modal inline-expansion pattern.
 * This file is kept to avoid import errors; it renders nothing.
 */
function ShareModal() { return null; }
export default ShareModal;
