/**
 * CertDetailModal.jsx — STUB (no longer used)
 * All certificate detail is rendered inline inside WalletCertCard,
 * matching the existing app's zero-modal inline-expansion pattern.
 */
function CertDetailModal() { return null; }
export default CertDetailModal;
